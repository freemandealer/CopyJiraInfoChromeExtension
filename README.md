A Google Chrome Extension to copy info (summary, link, etc) from JIRA page to ClipBoard.

Go to chrome://extensions/, enable `developer mode` and use `Load unpacked` to load this extention from filesystem.

